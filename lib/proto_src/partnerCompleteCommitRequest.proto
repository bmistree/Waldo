import "util.proto";

/*
  Sent by root endpoint (and forwarded by subsequent endpoints) to
  finish second phase of commit.
 */
message PartnerCompleteCommitRequest
{
    required UUID event_uuid = 1;
}